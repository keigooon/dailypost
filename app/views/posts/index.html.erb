<% if logged_in? %>
  <h2>投稿一覧</h2>
  <%= render 'posts', posts: @posts %>
<% else %>
  <div class="center jumbotron">
    <div class="text-center">
      <h1>あなたの日常を投稿しよう</h1>
      <%= link_to '登録はこちらから', signup_path, class: 'btn btn-lg btn-primary' %>
    </div>
  </div>
<% end %>